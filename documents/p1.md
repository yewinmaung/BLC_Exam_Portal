You are working on a Laravel Exam Management System.

GOAL: Clean architecture + wire missing services + remove only safe legacy code
STEP 1 — LEGACY CLEANUP (SAFE ONLY)

Check Attempt Reset system:

AttemptResetController
AttemptResetRequest model
attempt_reset_requests table
admin attempt-reset views

TASK:

Verify if any routes use AttemptResetController
If NO routes exist → mark as DEAD CODE
If DB table is empty → mark for safe deletion

OUTPUT:

List what is SAFE TO DELETE
List what must be KEPT for now
STEP 2 — VERIFY REATTEMPT SYSTEM (DO NOT MODIFY LOGIC)

ReAttempt system must remain unchanged.

TASK:

Confirm all routes for ReAttemptController exist
Confirm ReAttemptService is used in controller
Confirm ReAttemptRequest + ReAttemptLog are fully connected
Check for missing view bindings or broken routes

OUTPUT:

Status: OK / BROKEN
Any missing links only (NO rewriting)
STEP 3 — SERVICE WIRING (DO NOT DELETE ANY SERVICE)

You MUST ONLY connect services to controllers (NO deletion).

Wire the following:

AcademicService
→ connect to AcademicYearController OR Student promotion flow
TranscriptService
→ connect to result/yearly transcript generation endpoint
CertificateService
→ connect to admin/teacher certificate issuance flow
QuestionImportService
→ connect to exam import controller (ExamController or dedicated import endpoint)

OUTPUT:

Which controller should call which service
Missing injection points
STEP 4 — RESULT SYSTEM STRUCTURE CHECK

Validate:

Result
YearlyExamResult
YearlyTranscript
AcademicYear
YearLevel

TASK:

Map relationships between these tables
Confirm support for:
academic year filtering
semester filtering
student-only access control

OUTPUT:

ERD (text format)
Missing relations (if any)
STEP 5 — FINAL OUTPUT FORMAT

Provide ONLY:

Safe Deletions List
Keep List
Service Wiring Map (Controller → Service)
Database Relationship Diagram (text ERD)
Broken/Missing Endpoints List
RULES
Do NOT modify business logic
Do NOT delete ReAttempt system
Only mark AttemptReset system for deletion (do not delete directly)
Only wire services (no refactor unless necessary)
Keep output structured and short